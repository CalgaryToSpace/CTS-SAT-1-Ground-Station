"""Main entry point for running the package like `python -m cts1_ground_support`."""

from packages.cts1_ground_support.src.cts1_ground_support.terminal_app.dash_gui import main

if __name__ == "__main__":
    main()
